import 'package:flutter/material.dart';

class ColorTemplates {
  static const Color buttonClr = Color.fromRGBO(2, 240, 2, 1);
  static const Color textClr = Colors.black;
  static const Color primary = Colors.white;
}
